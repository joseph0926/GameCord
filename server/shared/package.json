{
  "name": "@gamecord/shared",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf dist && rm -rf tsconfig.tsbuildinfo",
    "build": "npm run clean && tsc -b",
    "build:watch": "tsc -b -w",
    "test": "jest"
  },
  "devDependencies": {
    "@types/node": "^22.8.1",
    "@types/winston": "^2.4.4",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@nestjs/common": "^10.4.6",
    "rxjs": "^7.8.1",
    "winston": "^3.15.0",
    "winston-daily-rotate-file": "^5.0.0"
  }
}
